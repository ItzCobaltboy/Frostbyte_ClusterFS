# Activate test profile
spring.profiles.active=test

# Set this to indicate we're in test mode
spring.test.context=true

# Server config for tests
server.port=7000
server.address=localhost

# Disable schedulers in tests
spring.task.scheduling.enabled=false